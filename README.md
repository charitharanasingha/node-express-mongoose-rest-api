# RESTful API with Node.js, Express.js, Mongoose and MongoDB with Virtual Schema

Example of a RESTful API built with Node.js, Express.js, Mongoose and MongoDB with Virtual Schema


## Install

`npm install`

## Run
`npm run start`
